[package]
name = "goliath-search"
description = "Event searches for the Goliath security platform: a typed structure checked against the OCSF schema"
keywords = ["ocsf", "siem", "security", "search"]
categories = ["data-structures"]
readme = "README.md"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
# Not published until the API and interface have used it.
publish = false

[dependencies]
goliath-ocsf = { path = "../goliath-ocsf", version = "0.1.2" }
jiff = { version = "0.2", default-features = false, features = ["std"] }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[lints]
workspace = true
